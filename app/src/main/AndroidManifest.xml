<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bashkirov_kirill_taxi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bashkirov_Kirill_Taxi">
        <activity
            android:name=".MainActivity_settings"
            android:exported="false" />
        <activity
            android:name=".MainActivity_history"
            android:exported="false" />
        <activity
            android:name=".MainActivity_menu"
            android:exported="false" />
        <activity
            android:name=".MainActivity_start_screen"
            android:exported="false" />
        <activity
            android:name=".MainActivity_registration"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/okrygl_layout" />

        <activity
            android:name=".MainActivity_sign_in"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>